#  Setup Instructions for mini-netstack

##  Required Tools

Install the following on your Linux/WSL system:

```bash
sudo apt update
sudo apt install build-essential tcpdump wireshark net-tools iproute2 netcat iperf -y
